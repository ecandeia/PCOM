\chapter{Modulação em Ângulo}

Nos anos 20 a radiodifusão estava no início e se procurava uma forma de
transmissão que reduzisse o efeito do ruído (estática). Como
a potência do ruído é proporcional a largura de faixa do sinal modulado, e idéia
era encontrar um esquema de modulação que reduzisse a largura de faixa.

Vislumbrou-se então um novo método que eliminava as bandas laterais. A idéia
era variar a frequência com o sinal mensagem,
\[
\omega(t) = \omega_{c} + k m(t)
\]
sendo $k$ uma constante arbitrária. Acreditava-se que se $m(t)$ varia de $-m_p$ a $m_p$, então
os valores mínimo e máximo de $\omega(t)$ seriam  $\omega_{MIN}(t) = \omega_c - k m_p$ e
$\omega_{MAX}(t) = \omega_c + k m(t)$ respectivamente. Portanto,  as componentes
espectrais ficariam numa faixa de $2k m_p$ centradas em $\omega_c$. Como $k$
é uma constante, ela pode ser feita arbitrariamente pequena produzindo assim
uma faixa arbitrariamente pequena.

Infelizmente obervou-se na prática a largura do sinal modulado seguindo a regra acima
é na melhor das hipóteses igual a utilizada por um sinal AM. Neste capítulo
a técnica de modulação que varia a frequência de portadora em função
de mensagem é apresentada e é mostrado o que acontece para que o pensamento
descrito acima não esteja correto.

\section{Modulação em Ângulo ou Exponencial}

Como foi  mostrado a informação pode ser transmitida pela modificação das
características de uma senóide. No Capítulo anterior variando a amplitude
dessa senóide de acordo com a mensagem e mantendo sua frequência constante obtivemos a modulação em amplitude.
Agora vamos modificar a fase ou a frequência dessa senóide em função da mensagem.

Na modulação em frequência, desejamos variar a frequência da portadora em função
do sinal modulante $m(t)$. Ou seja, a frequência da portadora será modificada a
cada instante. É interessante que tenhamos um sinal senoidal com pelo menos um
ciclo na mesma frequência, e isto nos leva ao conceito de frequência instantânea.

Seja um sinal senoidal generalizado
\[
\phi(t) = A\cos\theta(t)
\]
sendo $\theta(t)$ o ângulo generalizado que é função de $t$. Para $\theta(t) = A\cos(\omega_{c}t
+ \theta_0)$, $\theta_t(t) = \omega_{c}t + \theta_0$, graficamente temos o ângulo generalizado representado
na Figura~\ref{fig:fighw}.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/angulogeneralizado.eps,width=6cm}}
\caption{\label{fig:fighw}Ângulo generalizado.}
\end{figure}
%*******************

Em um pequeno intervalo $\Delta t$ podemos dizer que $\omega_{c}t + \theta_0$
é a tangente de $\theta(t)$. Dessa forma sob esse pequeno intervalo tem-se
\[
\theta(t) = \omega_{c}t + \theta_0
\]
ou
\[
\phi(t) = A\cos(\omega_{c}t + \theta_0) \ \ t_1<t<t_2.
\]

Assim podemos dizer que no pequeno intervalo $\Delta t$ a frequência de $\phi(t)$
é $\omega_c$, ou de outra forma, que a frequência de $\phi(t)$ é a inclinação
da tangente de $\theta(t)$ no intervalo $\Delta t$. Generalizando para
qualquer intervalo $\gamma$, definimos a frequência instantânea $\omega_i$
como a inclinação de $\theta(t)$ no tempo $t$, ou
\[
\omega_i = \frac{d\theta}{dt}
\]
e
\[ \theta(t) = \int_{-\infty}^{t}\omega_i(\alpha)d\alpha.
\]

Veremos como transmitir $m(t)$ variando $\theta(t)$. Tal técnica é chamada de
modulação em ângulo ou exponencial. Para esse tipo de modulação existem duas possibilidades:
\begin{enumerate}
\item Modulação em fase (PM)
\item Modulação em frequência (FM)
\end{enumerate}


Na modulação em fase $\theta(t)$ varia linearmente com $m(t)$
\[
\theta(t) = \omega_{c}t + \theta_0 + k_p m(t)
\]
sendo $k_p$ uma constante, $\omega_{c}$ a frequência da portadora e
$\theta_0$ a fase de portadora que pode ser feita igual a zero sem perda
de generalidade, então
\[
\theta(t) = \omega_{c}t + k_p m(t)
\]
assim
\begin{equation}
 \label{eq:pmbasico}
s_{PM}(t) = A\cos[\omega_c t + k_p m(t)]
\end{equation}
e a frequência instantânea $\omega_i$ será
\[
\omega_i = \frac{d}{dt}[\omega_{c}t + k_p m(t)] = \omega_c + k_p m^{\prime}(t).
\]

Portanto, na modulação em fase a frequência instantânea varia linearmente com a
derivada do sinal modulante.

Se a frequência instantânea é que varia linearmente com o sinal modulante temos modulação
em frequência (FM), ou seja
\begin{eqnarray}
\omega_i = \omega_c + k_f m(t)
\end{eqnarray}
sendo $k_f$ uma constante, temos
\begin{eqnarray}
\theta(t) &=& \int_{-\infty}^{t}[\omega_{c} + k_f m(\alpha)]d\alpha \nonumber \\
&=& \omega_{c}t + k_f\int_{-\infty}^{t}m(\alpha)\alpha
\end{eqnarray}
donde,
\begin{equation}
s_{FM} = A\cos[\omega_{c}t + k_f\int_{-\infty}^{t}m(\alpha)d\alpha].
\label{eq:fmbasico}
\end{equation}

As equações~\ref{eq:pmbasico} e~\ref{eq:fmbasico} dos sinais PM e FM respectivamente são bastante similares, na verdade observando os sinais
PM e FM no tempo não é possível distinguí-los. Devido a dualidade entre FM e PM, se temos um
modulador de fase podemos gerar um modulador em frequência e vice-versa, como
está ilustrado na Figura~\ref{fig:modfreqfase}. O primeiro método foi usado na prática por Armstrong e era chamado de sistema
FM indireto (1936).

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/modfreqfase.eps,width=12cm}}
\caption{\label{fig:modfreqfase}Moduladores em Frequência e Fase.}
\end{figure}
%*******************



Isto sugere que podemos ter uma função generalizada, na qual FM e PM são casos
especiais, esta função é chamada de modulação exponencial (EM - {\em Exponential Modulation}) e é dada por
\[
s_{EM}(t) = A\cos\left[\omega_{c}t + k\int_{-\infty}^{\infty}m(\alpha)h(t-\alpha)
d\alpha\right]
\]
sendo $k$ uma constante. Se
\[
h(t) = \delta(t) \Rightarrow \int_{-\infty}^{\infty}m(\alpha)\delta(t-\alpha)d\alpha = m(t)
\]
temos um sinal PM, se
\[
h(t) = u(t) \Rightarrow \int_{-\infty}^{\infty}m(\alpha)u(t-\alpha)d\alpha = \int_{-\infty}^{t}m(\alpha)d\alpha
\]
temos um sinal FM.

\begin{exemplo}\label{ex:modulacaofm1}Desenhe as ondas FM e PM para o sinal $m(t)$ representado na
Figura abaixo. $k_f = 2\pi 10^5$, $k_p = 10\pi$ e $f_c = 100KHz$.
\end{exemplo}

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/extriang.eps,width=8cm}}
\caption{\label{fig:extrtiang}Função do Exemplo~\ref{ex:modulacaofm1}.}
\end{figure}
%*******************


{\bf Solução:} Em FM temos $\omega_i = \omega_c + k_f m(t)$ que dividindo por $2\pi$
é
\begin{eqnarray}
f_i &=& f_c + \frac{k_f}{2\pi}m(t) \nonumber \\
&=& 100\times 10^6 + \frac{2\pi 10^5}{2\pi}m(t) = 10^8 + 10^5 m(t) \nonumber
\end{eqnarray}
donde obtém-se $f_{i(min)} = 10^8 -10^5 = 99,9MHz$ e $f_{i(max)} = 10^8 + 10^5=100,1MHz$.


Como $m(t)$ cresce (ou decresce) linearmente com o tempo $f_i$ cresce (ou decresce) de
99,99MHz a 100,1MHz linearmente com o tempo.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/extriangfm.eps,width=8cm}}
\caption{\label{fig:extrtiangfm}Sinal do Exemplo~\ref{ex:modulacaofm1} modulado em frequência.}
\end{figure}
%*******************

b) PM: temos que PM para $m(t)$ é FM para $m^{\prime}(t)$, de forma que 
\begin{eqnarray}
f_i &=& f_c + \frac{k_p}{2\pi}m^{\prime}(t) \nonumber \\
&=& 10^8 + \frac{10\pi}{2\pi}m^{\prime}(t) = 10^8 + 5m^{\prime}(t) \nonumber
\end{eqnarray}
$m^{\prime}(t) = a$ quando $m(t)$ varia de -1 a 1 e $m^{\prime}(t) = -a$ quando $m(t)$
varia de 1 a -1, sendo que $a$ indica a inclinação da reta definida pelos pontos -1 a
1 em $t$, ou seja
\[
a = \frac{1-(-1)}{10^4} = 2\times 10^4 = 20.000
\]

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/extriangdeerivada.eps,width=8cm}}
\caption{\label{fig:extrtiangderivada}Derivada do sinal do Exemplo~\ref{ex:modulacaofm1}.}
\end{figure}
%*******************

$f_{i(min)} = 10^8 - 5\times 20.000 = 10^8 - 10^5 = 99,99MH$
$f_{i(max)} = 10^8 + 5\times 20.000 = 10^8 + 10^5 = 100,1MH$

Como $m^{\prime}(t)$ comuta de 20.000 a -20.000 a cada $10^{-4}s$ a $f_{i}$ comuta
de 100,1 e 99,9Mhz a cada $10^{-4} s$.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/extriangpm.eps,width=8cm}}
\caption{\label{fig:extrtiangpm}Sinal  modulado em Fase do Exemplo~\ref{ex:modulacaofm1}.}
\end{figure}
%*******************


\begin{exemplo}\label{ex:modulacaofm2} Desenhar ondas FM e PM para um sinal modulante digital $m(t)$ abaixo.
$k_f = 2\pi 10^5$, $k_p = \pi/2$ e $f_c = 100MHz$.
\end{exemplo}

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/exquadrada.eps,width=8cm}}
\caption{\label{fig:extquadrada}Sinal do Exemplo~\ref{ex:modulacaofm2}.}
\end{figure}
%*******************


\subsubsection{Solução:}
a) FM, temos 
\[
f_i = f_c + \frac{k_f}{2\pi}m(t) = 10^8 + 10^5m(t).
\] 
Como $m(t)$ comuta
de 1 a -1 e vice-versa, a onda Fm também de 100,1 a 99,9MHz

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/exquadradafm.eps,width=8cm}}
\caption{\label{fig:extquadradafm}Sinal do Exemplo~\ref{ex:modulacaofm2} modulado em FM.}
\end{figure}
%*******************

Este esquema de modulação é chamado de FSK ({\em Frequency-Shift Keying}) comutação
por deslocamento em frequência.

b) Para PM, temos 
\[
f_i = f_c + \frac{k_f}{2\pi}m^{\prime}(t) = 10^8 + \frac{1}{4}m^{\prime}(t). 
\]
e não fica claro como a $f_i$ se altera. Portanto vamos considerar a abordagem direta.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/exquadradadedrivada.eps,width=8cm}}
\caption{\label{fig:extquadradaderivada}Sinal do Exemplo~\ref{ex:modulacaofm2} derivado no tempo.}
\end{figure}
%*******************


\begin{eqnarray}
s_{PM}(t) &=& A\cos[\omega_{c}t + k_p m(t)] \nonumber \\
&=& Acos[\omega_{c}t + \frac{\pi}{2}m(t)] \nonumber
\end{eqnarray}

\begin{eqnarray}
s_{PM}(t) =
\left\{
\begin{array}{c}
A\cos[\omega_{c}t + \frac{\pi}{2}] = -A\sin\omega_{c}t \ \ para\ m(t)=1 \\
A\cos[\omega_{c}t - \frac{\pi}{2}] = A\sin\omega_{c}t \ \ para\ m(t)=-1 \nonumber
\end{array}
\right.
\end{eqnarray}

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/exquadradapm.eps,width=8cm}}
\caption{\label{fig:extquadradapm}Sinal do Exemplo~\ref{ex:modulacaofm2} modulado em fase.}
\end{figure}
%*******************

Este esquema é chamado PSK ({\em Phase-Shift Keying}) comutação por deslocamento
de fase. Observe que ele pode ser visto como uma modulação DSB-SC por $m(t)$.

A onda $s_{PM}(t)$ tem descontinuidade nos instantes onde os impulsos em $m^{\prime}(t)$
estão localizados. Isto implica que $\omega_{i} = \frac{d\Theta}{dt}$ nestes instates
$\rightarrow \infty$.

É interessante notar que $k_p$ deve ser escolhido de forma que a fase fique restrita a faixa $(-\pi,\pi)$.
Por exemplo, se $k_p = \frac{3\pi}{2}$ em nosso exemplo
\[
s_{PM}(t) = A\cos[\omega_{c}t + \frac{3\pi}{2}m(t)]
\]
e $s_{PM}(t) = A\sin\omega_{c}t$ quando $m(t)=1$ e $m(t)=-1/3$ o que pode causar ambiguidades
no receptor.

Se $m(t)$ for contínuo a restrição sobre $k_p$ não é necessária. Com isso podemos dizer que 
para $m(t)$ limitado em faixa $k_p$ não tem qualquer restrição.

\section{Potência de uma Onda Modulada em Ângulo}

Embora $\omega_i$ varie como tempo, a amplitude $A$ permanece sempre a mesma. Portanto
$P_{s_{PM}} = P_{s_{FM}} = \frac{A^2}{2}$ independente de $k_p$ e $k_f$.

\section{Largura de Faixa de Sinais Modulados em Ângulo}

Primeiro caso de FM, vamos ter
\[
a(t) = \int_{-\infty}^{t}m(\alpha)d\alpha
\]
assim
\begin{eqnarray}
s_{FM}(t) &=& A\cos(\omega_{c}t + k_{f}a(t)) \nonumber \\
&=& Re[Ae^{[j(\omega_{c}t+ k_{f}a(t))]}] \nonumber \\
&=& Re[\hat{s}_{FM}(t)]
\end{eqnarray}
agora
\[
\hat{s}_{FM}(t) = Ae^{j\omega_{c}t}e^{jk_{f}a(t)}
\]
expandido em série este último termo
\[
\hat{s}_{FM}(t) = Ae^{j\omega_{c}t}[1 + jk_{f}a(t) - \frac{k_{f}^{2}}{2!}a^{2}(t) + ... + \frac{j^{n}k_{f}^{n}a^{n}(t)}{n!} + ...]
\]
tomando a parte real 
\begin{eqnarray}
\hat{s}_{FM}(t) &=& A\cos[1 - \frac{k_{f}^{2}}{2!}a^{2}(t) + ...] + A\sin\omega_{c}t[-k_{f}a(t) + \frac{k_{f}^{3}a^{3}(t)}{3!} - ...] \nonumber \\
&=& A[\cos\omega_{c}t - k_{f}a(t)\sin\omega_{c}t - \frac{k_{f}^{2}a^{2}(t)}{2!}\cos\omega_{c}t + \frac{k_{f}^{3}a^{3}(t)}{3!}\sin\omega_{c}t + ...]
\end{eqnarray}

Se $M(\omega)$ é limitado em faixa em $B Hz$ então $M(\omega)\frac{1}{j\omega} = A(\omega)$ (integral)
também é limitada em $B Hz$.
\begin{eqnarray}
a^2(t) &\leftrightarrow & A(\omega) * A(\omega)/2\pi \ limitada\  em \ 2B Hz \nonumber \\
a^{n}(t) &\leftrightarrow & A(\omega) * A(\omega)* ... A(\omega)/(2\pi)^{n-1} \ limitada\ em \ n B Hz. \nonumber \\
\end{eqnarray}

De modo que o espectro total é o espectro da portadora mais o espectro dos $a^{n}$ centrados
em $\omega_c$. É fácil notar que este espectro é ilimitado em faixa.

Embora teoricamente a largura de faixa de uma onda FM seja infinita, podemos notar que a
maior parte da potência do sinal está contida numa faixa finita.
Usando essa informação tem-se dois casos a considerar:
\begin{itemize}
\item FM de faixa estreita, e 
\item FM de faixa larga
\end{itemize}

\section{Modulação em Ângulo em Faixa Estreita}

A modulação em ângulo é não linear, por exemplo
\[
A\cos\{\omega_{c}t + k_{f}[a_{1}(t) + a_{2}(t)]\} \neq A\cos[ \omega_{c}t + k_{f}a_{1}(t)] 
+ A\cos[ \omega_{c}t + k_{f}a_{2}(t)]
\]
para confirmar ese fato basta a expansão do cosseno.

Agora se fizermos $k_f$ muito pequeno de modo qe $\mid k_{f} a(t)\mid << 1$ então na expansão
em série os termos 
\[
\frac{k_{f}^{2}a^{2}(t)}{2!},\ \frac{k_{f}^{3}a^{3}(t)}{3!},\ ...,\ \frac{k_{f}^{n}a^{n}(t)}{n!}
\]
tendem a zero e ficamos com
\[
s_{FM} \approx A[\cos\omega{c}t - k_{f}a(t)\sin\omega_{c}t]
\]
que é linear. Esta expressão é semelhante a de uma onda AM. Como $a(t)$ é limitado em
$B Hz$, $s_{FM}(t)$ terá apenas $2B Hz$. Por esa razão, quando $\mid k_{f}a(t)\mid << 1$
este caso é chamado de {\bf FM de faixa estreita} (NBFM, {\em Narrow Band FM}).
O PM de faixa estreita é similar
\[
s_{PM}(t) \approx A[\cos\omega_{c}t - k_{p}m(t)\sin\omega_{c}t].
\]

As equações acima sugerem um método possível para gerar NBFM e NBPM usando moduladores DSB-SC,
como está ilustrado na Figura~\ref{fig:fmpmusandodsb}.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/fmpmusandodsb.eps,width=8cm}}
\caption{\label{fig:fmpmusandodsb}Método para gerar NBFM e NBPM via DSB-SC.}
\end{figure}
%*******************

\begin{exercicio}
 Encontre as expressões para os espectros de $s_{FM}(t)$ e $s_{PM}(t)$ considerando faixa estreita.
\end{exercicio}


Se $\mid k_{f}a(t)\mid$ não é  muito menor que 1, não podemos desprezar os termos de mais
alta ordem da equação do sinal FM, e a análise se torna bastante complicada.

%Vamos seguir o raciocínio dos pioneiros e descobrir onde eles erraram para obter uma
%estimativa melhor da largura de faixa do WBFM.

Para obter uma estimativa vamos considerar  $m(t)$ limitado em $B Hz$. Este sinal pode ser aproximado por digamos
$\hat{m}(t)$ como mostra a Figura~\ref{fig:sinalaproximado}.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/sinalaproximado.eps,width=8cm}}
\caption{\label{fig:sinalaproximado}Aproximação do sinal $m(t)$ por degraus.}
\end{figure}
%*******************

Por conveninência cada pulso será chamado de um célula. É relativamente fácil analisar FM
pra $\hat{m}(t)$ por causa das amplitudes constantes de cada célula. Para assegurar que
$\hat{m}(t)$ contenha toda a informação de $m(t)$ cada célula nao deve ser
maior que o intervalo de Nyquist $1/2B$ segundos.


Vamos considerar uma célula típica comecando em $t = t_k$, esta célula tem um amplitude
constante $m(t_k)$, portanto o sinal FM correspondente a esta célula será uma senóide
de frequência
\[
\omega_i = \omega_{c} + k_{f}m(t_{k})\ para\ t_k<t<t_k+\frac{1}{2B}
\]
como mostrado na Figura~\ref{fig:sinalcelula}.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/sinalcelula.eps,width=6cm}}
\caption{\label{fig:sinalcelula}Sinal FM correspondente a uma célula.}
\end{figure}
%*******************

O sinal FM consiste de uma sequência de pulsos correpondentes a cada célula de $\hat{m}(t)$.
O espectro FM de $\hat{m}(t)$ consiste da soma das transformadas de Fourier dos pulsos.
A transformada de Fourier de um pulso senoidal é uma função $sinc$, representado na
Figura~\ref{fig:sinccelula}.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/sinccelula.eps,width=8cm}}
\caption{\label{fig:sinccelula}Transformada de Fourier de um pulso senoidal}
\end{figure}
%*******************

Note que o espectro deste pulso é espalhado ao redor de $\omega_c + k_{f}m(t_k)$ por
$\frac{2\pi}{T} = \frac{2\pi}{1/2B} = 4\pi B$.

As amplitudes máxima e mínima das células são respectivamente $-m_p$ e $m_p$.
Portanto as frequências máxima e mínima do sinal FM são $\omega_c + k_{f}m_p$ e
$\omega_c - k_{f}m_p$ respectivamente. O espectro de cada pulso com frequência máxima e mínima
é mostrado na Figura~\ref{fig:sincceluladuplo}.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/sincceluladuplo.eps,width=8cm}}
\caption{\label{fig:sincceluladuplo}Transformada de Fourier de dois pulsos senoidais.}
\end{figure}
%*******************

Portanto as componentes de frequências máxima e mínima significativas serão
\[
\omega_c + k_{f}m_p + 4\pi B\ \ e\ \ \omega_{c} - k_{f}m_p - 4\pi B
\]
com uma largura de faixa de 
\[
2k_{f}m_p + 8\pi B
\]
considerando que a potência do sinal está contida no lóbulo principal 
da função $sinc$.

As frequências máxima e mínima da portadora são $\omega_c + k_f m_p$ e 
$\omega_{c} - k_{f}m_p$, mas suas componentes espectrais só estariam nessa faixa
se essas portadoras fossem senóidais que sempre existiram e com as frequências
constantes nesse caso impulsos em $\omega_c + k_{f}m_p$ e $\omega_c - k_{f}m_p$.
Para senóides com duração finita de $T$ segundos o espectro é espalhado por $2\pi/T$.
%Os pioneiros esqueceram esse efeito do espalhamento, o que os levou a dizer que
%a banda ocupada era pequena, como foi dito na introdução deste capítulo.

O desvio de frequência da portadora é de $\pm k_{f}m_p$. Chamamos o desvio de
frequência de $\Delta\omega$, assim
\[
\Delta\omega = k_{f}m_p
\]
em Hertz
\[
\Delta_f = \frac{\Delta\omega}{2\pi} = \frac{k_{f}m_p}{2\pi}.
\]

A largura de faixa estimada para FM pode ser expressa como:
\[
B_{FM} = \frac{1}{2\pi}(2k_{f}m_p + 8\pi B) = 2(\frac{k_{f}m_p}{2\pi} + \frac{4\pi B}{2\pi}) = 2(\Delta f + 2B)
\]
Esta estimativa é um pouco maior que o valor real pois foi obtida para $\hat{m}(t)$ e não para
$m(t)$ que é um sinal mais suave. Portanto vamos reajustar a nossa estimativa.

Primeiro vamos analizar nossa equação para NBFM, ou seja $k_f$ muito pequeno, em outras palavras
$\Delta f$ é muito pequeno em relação a $B$, de modo qu epodemos ignorá-lo, assim $B_{FM} \approx 4B$.

Mas tinhamos visto anteriormente que para esse caso a largura de faixa seria $2B$. Isto indica que
uma estimativa melhor é
\[
B_{FM} = 2(\Delta f + B) = 2\left(\frac{k_{f}m_p}{2\pi} + B \right)
\]
que é o resultado obtido por Carlson, que investigou o problema rigorosamente para um tom.
Por isso esse fórmula é chamada de {\bf Regra de Carson}.

Agora se $\Delta_f >> B$, caso de um verdadeiro WBFM $B_{FM} \approx 2\Delta_f$. Como
$\Delta\omega = 2\pi\Delta_f = \frac{2\pi k_{f}m_p}{2\pi} = k_{f}m_p$, para
esse caso isso é precisamento o que foi reconhecido pelo pioneiros. O único erro foi
pensar que funcionava par qualquer caso, especialmente quando $\Delta f << B$.


Definimos a regra de desvio $\beta = \frac{\Delta f}{B}$ e assim a regra de Carson fica
\[
B_{FM} = 2B(\beta + 1)
\]
$\beta$ controla a quantidade de modulação e tem um papel semelhante ao ínidice de
modulação do AM, e por isso é também chamado de índice de modulação para FM.

\section{Modulação em Fase}

Todos os resultados derivados para o FM podem ser aplicados ao PM. Vejamos:
\[
\omega_i = \omega_c + k_{p}m^{\prime}(t)
\]
O desvio de frequência $\Delta\omega = k_{p}m^{\prime}(t)$.

Assumimos que $m^{\prime}_p = [m^{\prime}(t)]_{MAX} = [m^{\prime}(t)]_{MIN}$, portanto,
\[
B_{PM} = 2(\Delta f + B) = 2\left( \frac{k_{p}m^{\prime}_p}{2\pi} + B \right).
\]

Um aspecto importante é que $\Delta\omega$ para o FM depende só do valor de pico de $m(t)$, $m_p$,
e não do espectro de $m(t)$.

Agora no caso da PM, $\Delta\omega = k_{p}m^{\prime}_p$, depende do valor de pico de $m^{\prime}(t)$
que depende fortemente do espectro e frequências de $m(t)$. Variações rápidas de $m(t)$ implicam
em $m^{\prime}$ maiores.

Finalmente pode-se concluir que
\begin{itemize}
\item WBFM (largura de faixa) independe do espctro de $m(t)$ ($B$ é quase desprezível) e
\item WBPM é fortemente dependendente do espectro de $m(t)$
\end{itemize}

\section{Verificação da Relação de Largura da Faixa FM}

Vamos analizar para o caso espcífico de um tom, $m(t) = \alpha\cos\omega_{m}t$, assim
\[
a(t) = \frac{\alpha}{\omega_m}\sin\omega_{m}t\ \ assumimos\ a(-\infty)=0
\]
e 
\[
\hat{\varphi}_FM(t) = Ae^{j(\omega_{c}t + k_{f}a(t))} = Ae^{j(\omega_{c}t + \frac{k_{f}\alpha}{\omega_m}\sin\omega_{m}t)}
\]
sendo $\Delta\omega = k_{f}m_{p} = \alpha k_f$.

A largura da faixa de $m(t)$ é $B=f_M Hz$. O índice de modulação 
\[
\beta = \frac{\Delta f}{f_m} = \frac{\Delta\omega}{\omega_m} = \frac{\alpha k_f}{\omega_m}
\]
portanto,
\begin{eqnarray}
\hat{\varphi}_{FM}(t) &=& A e^{j(\omega_{c}t + \beta\sin\omega_{m}t)} \nonumber \\
&=& Ae^{j\omega_{c}t}e^{j\beta\sin\omega_{m}t}
\end{eqnarray}
Este último termo é periódico com período $2\pi/\omega_m$ e portanto pode ser expandido em série de
Fourier
\[
e^{j\beta\sin\omega_{m}} = \sum_{n=-\infty}^{\infty}C_n e^{jn\omega_{m}t}
\]
sendo 
\[
C_n = \frac{1}{T}\int_{-T/2}^{T/2}e^{j\beta\sin\omega_{m}t}e^{-j\beta\sin\omega_{m}t}dt\ \ como\ \ T=\frac{2\pi}{\omega_m}
\]
\[
C_n = \frac{\omega_m}{2\pi}\int_{-\pi/\omega_m}^{\pi/\omega_m}e^{j\beta\sin\omega_{m}t-jn\omega_{m}t}dt
\]
fazendo $\omega_{m}t = x$, $\omega_{m}dt = dx$ e os limites de integraçao
$x_{i} = \omega_{m}\left(\frac{-\pi}{\omega_m}\right) = -\pi$ e 
$x_{s} = \omega_{m}\left(\frac{\pi}{\omega_m}\right) = \pi$
assim
\[
C_n = \frac{1}{2\pi}\int_{-\pi}^{\pi}e^{j(\beta\sin(x-nx)}dx
\]
esta integral não pode ser avaliada diretamente, porém pode-se expandir o termo mais interno numa
série infinita. Esta intergral se encontra tabulada e é chamada de função de Bessel 
de primeira classe e ordem $n$, ou seja, $C_n = J_n(\beta)$, representadas na Figura~\ref{fig:jacobianos}.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/jacobianos.eps,width=8cm}}
\caption{\label{fig:jacobianos}Função $J_n(\beta)$ para $\beta =1,\ 2,\ 3\ e\ 4$.}
\end{figure}
%*******************

$J_n(\beta)$ é fornecida para vários valores de $\eta$ e $\beta$, assim
\begin{eqnarray}
e^{j\beta\sin\omega_{m}t} &=& \sum_{n=-\infty}^{\infty}C_{n}e^{jn\omega_{m}t} \nonumber \\
&=& \sum_{n=-\infty}^{\infty}J_{n}(\beta)e^{jn\omega_{m}t}
\end{eqnarray}
substituindo em $\hat{\varphi}_{FM}(t)$ temos
\begin{eqnarray}
\hat{\varphi}_{FM}(t) &=& Ae^{j\omega_{c}t}\sum_{n=-\infty}^{\infty}J_{n}(\beta)e^{jn\omega_{m}t} \nonumber \\
&=& A\sum_{n=-\infty}^{\infty}J_{n}(\beta)e^{j(\omega_c + n\omega_{m})t} 
\end{eqnarray}
donde
\begin{eqnarray}
\hat{\varphi}_{FM}(t) &=& Re[\hat{\varphi}_{FM}(t)] \nonumber \\
&=& A\sum_{n=-\infty}^{\infty}J_n(\beta)\cos(\omega_c + n\omega_m)t
\end{eqnarray}

Vemos que o sinal modulado tem uma componente correspondente a portadora quando $\eta = 0 $ e 
infinitas componentes nas bandas laterais, $\omega_c \pm \omega_m$, $\omega_c \pm 2\omega_m$, ...,
$\omega_c \pm n\omega_m$,...

Para cada $\beta$ o comprimento de $n$-ésima componenete $\omega = \omega_c + n\omega_m$
é $J_n(\beta)$ para $\omega = \omega_c - n\omega_m$ é $J_n(-\beta) = (-1)^{n} J_{n}(\beta)$.

Como mostrado no gráfico $J_n(\beta)$ decresce com $n$ de modo que para $n$ grande o 
suficiente $J_n(\beta)$ se torna desprezível. Em geral $J_n(\beta)$ é desprezíval para
$\eta>\beta + 1$, assim temos $\beta + 1$ bandas laterais significativas. A largura de
faixa significativa para o FM nesse caso será
\[
B_{FM} = 2n f_m = 2(\beta + 1) f_m
\]
como $\beta = \frac{\Delta f}{f_m}$,
\begin{eqnarray}
B_{FM} &=& 2\left(\frac{\Delta f}{f_m} + 1\right)f_m \nonumber \\
&=& 2(\Delta f + \beta)\ \ onde\ \ B= f_m
\end{eqnarray}
que satisfaz a resultado anterior
NBFM $\beta << 1 \Rightarrow B_{FM} \approx 2f_m = 2B$,

Infelizmente a modulação em ângulo é não linear, ao contrário do AM, de modo que
os resultados para um tom não podem ser extrapolados para outros sinais (somatório
de tons).

Este método pode ser genrealizado para acharmos o espectro de um sinal
periódico qualquer $m(t)$.
\[
\hat{\varphi}_{FM}(t) = Ae^{j\omega_{c}t}e^{jk_{f}a(t)}
\]
$e^{jk_{f}a(t)}$ será também periódico e poderá ser expresso como uma série
de Fourier.

\begin{exemplo}
\begin{itemize}
\item[a)] Estimar $B_{FM}$ e $B_{PM}$ para $m(t)$ da Figura~\ref{fig:extrtiang}, com $k_f = 2\pi 10^5$ e $k_p = 5\pi$
\item[b)] Repita o problema se a amplitude de $m(t)$ dobrar
\end{itemize}
\end{exemplo}


\subsubsection{Solução:}

 a) Primeiro determinar a largura de faixa exponencial (B) de $m(t)$.
Como $m(t)$ é um sinal periódico pode ser representado por uma série de Fourier
\[
m(t) = \sum_{n} C_n\cos n\omega_{0}t\ \ onde\ \ \omega_0 = \frac{2\pi}{T} = \frac{2\pi}{2\times 10^{-4}} = 10^{4}\pi
\]
\begin{equation}
C_n = \left\{
\begin{array}{c}
\frac{8}{8\pi^{2}n^{2}}\ \ n\ \ ímpar \\
0 \ \ n\ \ par
\end{array}
\right.
\end{equation}
as amplitudes das harmônicas decrescem rapidamente com $n$. Por exemplo a terceira harmônica
tem apenas 11\% da fundamental e a quinta harmônica tem 4\% da fundamental. Em termos de potência
a terceira harmônica tem $0,11^2$ ou 1,21\% da potência da fundamental e 
a quinta harmônica tem apenas 0,16\%.

Assumindo então que a largura de faixa de $m(t)$ compreende até a terceira harmônica,
$\omega_m = 3\omega_0 = 3\times 10^4\pi$ ou $B= \frac{3\times 10^4\pi}{2\pi} = 15Khz$.

Para FM temos que $m_p = 1$ e pela regra de Carson
\[
B_{FM} = 2(\Delta f + B) = 2(100+15) = 230KHz
\]
de outra forma, o índice de modulação
\[
\beta = \frac{\Delta f}{B} = \frac{100}{15}
\]
e
\[
B_{FM} = 2B(\beta + 1) = 2.15(\frac{100}{15} + 1) = 230Khz
\]

Para PM já vimos que $m^{\prime} = 20.000$ então
\[
\Delta f = \frac{1}{2\pi} = \frac{1}{2\pi}5\pi 20.000 = 50KHz
\]
e $B_{PM} = 2(\Delta f + B) = 130KHz$. Pelo ínidice de modulação
$\beta = \frac{\Delta f}{B} = \frac{50}{15}$
\[
B_{PM} = 2B(\beta + 1) = 2\times 15(\frac{50}{150} + 1) = 130KHz
\]

b) Dobrando $m(t)$, $m_p = 2$ e $B=15KHz$ como antes.

Para FM:
\[
\Delta f = \frac{1}{2\pi}k_{f}m_p = 200KHz
\]
e
\[
B_{FM} = 2(\Delta f + B) = 430KHz \ \ e \ \ \beta=\frac{200}{15}
\]

Para PM $m^{\prime} = 40.000$
\[
\Delta f = \frac{1}{2\pi}k_{p}m^{\prime}_{p} = 100Khz
\]
\[
B_{PM} = 2(\Delta f + B) = 230KHz\ \ \ \beta=\frac{100}{15}
\]

Observe que duplicar $m(t)$ quase dobra a largura de faixa para FM e PM.

\begin{exemplo}Repetir o exemplo anterior para $T=4\times 10^{-4}$, duas
vezes o período anterior
\end{exemplo}

\subsubsection{Solução:}

Para determinar a nova largura de faixa essencial de $m(t)$, relembremos que 
uma expansão de 2 no tempo reduz o espectro de 2.
\[
\omega_0 = \frac{2\pi}{T} = 2,5\pi 10^4\ \ ou\ \ f_0 = 0,25\times 10^4 = 2,5KHz
\]
a terceira hermônica $B=3f_0 = 7,5KHz$

Para FM, $m_p = 1$ tem-se $\Delta f = \frac{1}{2\pi}k_{f}m_p = 100KHz$ e 
$B_{FM} = 2(\Delta f + B) = 2(100+7,5) = 215KHz$.

Para PM, $m_p^{\prime} = 10.000$ metade do anterior tem-se $\Delta f = \frac{1}{2\pi}k_{f}m_p^{\prime} = 25KHz$ e 
$B_{PM} = 2(\Delta f + B) = 2(25+7,5) = 65KHz$.

Observe que a expansão no tempo quase não afetou $B_{FM}$, mas reduziu a metade $B_{PM}$.
Isto ratifica nossa afirmação que o espectro do PM e fortemente do espectro
de $m(t)$.

\begin{exemplo} Um sinal modulado em ângulo com $\omega_c = 2\pi\times 10^5$
e dado por
\[
\varphi_{FM}(t) = 10\cos(\omega_{c}t +5\sin 3000t + 10\sin 2000t)
\]
\begin{itemize}
\item[a)] Ache a potência,
\item[b)] $\Delta f$,
\item[c)] $\beta$,
\item[d)] $\Delta\phi$ (desvio de fase)
\item[e)] Estime a largura de faixa
\end{itemize}
\end{exemplo}

\subsubsection{Solução:}

Determinar a largura do sinal mensagem (ou sua derivada) que é a maior
frequência de
\begin{eqnarray}
m_{PM}(t) &=& 5\sin 3000t + 10\sin 2000\pi t \nonumber \\
m_{FM}(t) &=& 15000\cos 300t + 2000\pi\cos 2000\pi t \nonumber \\
\omega_{m} &=& 2000\pi\ \ ou\ \ B=\frac{\omega_m}{2\pi}=\frac{2000\pi}{2\pi}=100Hz \nonumber
\end{eqnarray}
\begin{itemize}
\item[a)]
\begin{equation}
P = \frac{A^2}{2} = \frac{10^2}{2} = 50
\end{equation}
\item[b)] Para achar $\Delta f$, precisamos achar $\omega_i$
\[
\omega_i = \frac{d\theta}{dt} = \omega_c + 15.000\cos 3000t + 20.000t\cos 2000\pi t
\]
desvio máximo quando $\cos 3000t$ e $\cos 2000\pi t$ iguais e a um ao mesmo tempo,
assim
\[
\Delta\omega = 15000+20000\pi \Leftarrow \Delta f = \frac{\Delta\omega}{2\pi} = 12.387,32 Hz
\]
\item[c)]
\[
\beta = \frac{\Delta f}{B} = \frac{12.3987,32}{1.000} = 12,387
\]
\item[d)]
\[
\Theta(t) = \omega_{c}t + (5\sin 3000t + 10\sin 2000t)
\]
o desvio de fase $\Delta\theta$ é o máximo valor de $m(t)$ ou seja quando
ambos os senos forem 1, assim $\Delta\theta = 5 + 10 = 15 rad$.
\item[e)] 
\begin{eqnarray}
B_{FM} &=& 2(\Delta f + B) \nonumber \\
&=& 2(12.387,32 + 1000) = 26.774,65 Hz \nonumber
\end{eqnarray}
observe que não é necessário saber se o sinal é FM ou PM para determinar a largura
de faixa de $\varphi_{EM}(t)$.
\end{itemize}

\section{História de Edwin H. Armstrong (1890-1954)}


No início, o desenvolvimento do FM baseava-se na tentativa de diminuir a largura de faixa
de transmissão o que implicaria na diminuição da influência do ruído branco.

Os resultados experimentais mostraram que isto não era possível. Carson mostrou matematicamente
que no máximo se conseguia a largura de faixa do AM.

Infelizmente Carson não mostrou as vantagens do FM. E mais tarde escreveu num artigo: ``De
fato, tanto mais e mais esquemas são analisados e testados, quanto mais a natureza
essencial do problema persiste, nos estamos inevitavelmente forçados a conlcuir que 
a estática (ruído), como os pobres, sempre estará conosco''.

Esta opinião, de um mais habilidosos matemáticos da época na área de
comunicações, atrasou por uma década o desenvolvimento do FM.

As vantagens na supressão de ruídos do FM foi provada pelo Major Edwin H. Armstrong,
considerado como o pai do FM moderno, em um artigo de 1936.

Além da invenção de equipamentos de FM, ele contribuiu com o circuito de realimentação
regenerativa, que tornou a amplificação de rádio difusão possível, o circuito superheterodino,
base para todos os rádios modernos, e o circuito super-regenerativo usado em sistemas
de ondas curtas.

Armstrong foi um dos últimos inventores solitários de garagem. Para estabelecer a rádio difusão
por FM travou uma batalha com o pessoal de rádio difusão já estabelecido.

Em 1944 o FCC (órgão regulador de comunicações dos EUA), com base em um depoinmento de um técnico
especialista, trocou a faixa de frequências estabelecida para o FM de 42-50MHz para 
88-108MHz.
Isto tornou obsoletos todos os equipamentos (transmissores, receptores, antenas, etc.) construídos
para a antiga faixa de FM.

Armstrong lutou contra a decisão até que m 1947 consegiu a retratação do técnico. Mas
apesar de tudo, o FCC não trocou a faixa do FM.

A indústria de radiodifusão, antes resistente ao FM, passou a construir equipamentos 
para a nova faixa sem pagar ``royalties'' para Armstrong. Ele passou metade de 
sua vida nas cortes americanas na tentativa de separar a injustiça, chegando em 
ao suicídio em 1954 pulando da janela do $13^o$ andar de um prédio.

\section{Características da Modulação em Ângulo}

Como PCM o FM também pode utilizar a característica de negociar faixa de
frequência com potência do sinal, a largura de faixa pode ser ajustada pelo desvio 
de frequência $\Delta f$. A AM não tem esta característica. Isto é extremamante 
imporante em sistemas de comunicações espaciais.

\subsection{Imunidade a Não Linearidades}

Uma característica interessante de modulação em ângulo é a amplitude constante
(da portadora), isto a torna menos suscetível a não linearidades, vejamos
\[
y(t) = a_{1}x(t) + a_{2}x^{2}(t)
\]
um dispositivo não linear, se
\[
x(t) = \cos[\omega_{c}t + \psi(t)]
\]
então
\begin{eqnarray}
y(y) &=& a_{1}\cos[\omega_{c}t + \psi(t)] + a_2\cos^{2}[\omega_{c}t + \psi(t)] \nonumber \\
&=& \frac{a_2}{2} + a_1\cos[\omega_{c}t + \psi(t)] + a_2\cos[2\omega_{c}t + 2\psi(t)] \nonumber
\end{eqnarray}

Pra FM $\psi(t) = k_{f}\int m(\alpha)d\alpha$, donde
\begin{eqnarray}
y(t) &=& \frac{a_2}{2} + a_1\cos[\omega_{c}t + k_{f}\int m(\alpha)d\alpha] + a_2\cos[2\omega_{c}t + 2k_{f}\int m(\alpha)d\alpha] \nonumber
\end{eqnarray}

Filtrando o nível DC ($a_2/2)$, observe que o último termo tem a frequência da portaora e o
desvio de frequência multiplicado por 2. O termo em $a_1$ é o sinal FM. Em ambos
os casos $m(t)$ permanece intacto. Isto sugere que podemos fazer multiplicadores
de frequência para sinais FM com dispositivos não lineares sem distorcermos a informaçao.
Esses dispositivos não lineares são chamados de multiplicadores de frequência, por essa causa.
Qualquer dispositivo não linear tais como um diodo ou um transistor pode ser usado para isso.

De uma maneira geral dispositivos de ordem $n$ produzem uma multiplicação da $f_c$ e $\Delta f$
por $n$. Filtros pass faixas sintonizados em $n\omega_c$ podem gerar um sinal FM nessa frequência
como um desvio $\Delta f$ em relação ao sinal original.

Já vimos que para o AM-DSB-SC por exemplo não linearidades distorcem o sinal mensagem,
por exemplo
\[
y(t) = a x(t) + b x^{3}(t)
\]
sendo $x(t) = m(t)\cos\omega_{c}t$ produz
\[
y(t) = [a m(t) + \frac{3b}{4}m^{3}]\cos\omega_{c}t + \frac{b}{4}m^{3}(t)\cos 3\omega_{c}t
\]

Essa imunidade a não linearidades é a razão primária para modulação em ângulo ser
usad em sistemas repretidores de microondas que necessitem de níveis altos
de de potência. Isto impõe amplificadores não lineares classe C.

\section{Geração de Ondas FM}

Dois métodos serão apresentados: indireo e direto.

\subsection{Método Indireto de Armstrong}

Já vimos um método indireto para NBFM a partir da equação do PM
\[
\varphi_{PM}(t) \approx A[\cos\omega_{c}t - k_{f}a(t)\sin\omega_{c}t]
\]
que é uma simplificação quando $\mid k_p m(t)\mid <<1$.

Integrando $m(t)$ antes de introduzí-lo no modulador em fase produzindo
\[
\varphi_{FM} \approx A[\cos\omega_c t - k_fa(t)\sin\omega_c t]
\]
sendo $a(t) = \int_{-\infty}^{t}m(\alpha)d\alpha$.

Um sistema WBFM pode ser gerado a partir de um NBFM com a utilização
de mutliplicadores de frequência vistos anteriormente, lembrando que
tanto a frequência da portadora bem como o desvio de frequência são mutliplicados.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/wbfmindireto.eps,width=8cm}}
\caption{\label{fig:wbfmdireto}Gerador FM indireto de Armstrong.}
\end{figure}
%*******************

O método de geração do NBFM sugerido por Armstrong, apresenta distorções por causa
das aproximaçõaes realizadas. A sída do NBFM tem alguma modulação em amplitude.
A limitação da amplitude pelos multiplicadores de frequência removem 
grande parte de distroção.

\begin{exemplo} Temos um gerador NBFM com $f_{c1} = 200KHz$. Nesta frequência
é fácil construir osciladores de cristal estáveis. O desvio de frequência
$\Delta f_1 = 25Hz$, o espectro do sinal de áudio para obter alta fidelidade,
é de 50Hz a 15KHz. Desse modo $\beta = \frac{\Delta f_1}{f_m} = \frac{25}{15.000} <<1$,
no pior caso um sinal de voz $\beta = 25/50 = 0,5$.

A saída do transmissor comercial FM é de 91,2MHz e $\Delta f = 75KHz$.
\end{exemplo}

Para conseguirmos $\Delta f = 75KHz$, precisamos multiplicar de 75.000/25 = 3000 vezes
$\Delta f_1$.

Isto pode ser feito por dois estágio de multiplicaçao de 64 e 48, ou 
$64\times 48 = 3072$ o que produz $\Delta f = 76,8$. O valor exato $\Delta f = 75KHz$
pode ser conseguido com $\Delta f1 = 24,41Hz$. Os multiplicadores são 
projetados por meio de duplicadores e triplicadores colocados em cascata.
Assim $64 = 2^6$ (6 duplicadores), $48 = 2^4\times 3$(4 duplicaores, 1 triplicador).

O problema é essa multiplicação produz $f_c = 3072\times f_{c1} = 3072\times 200KHz
\approx 600MHz$.

Isto pode ser evitado pelo uso de conversores de frequência. O esquema
está represntado na Figura~\ref{fig:conversorfrequencia}.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/conversorfrequencia.eps,width=12cm}}
\caption{\label{fig:conversorfrequencia}Esquema do Gerador FM utilizando conversor de frequência.}
\end{figure}
%*******************

Vamos analisar o circuito, em a) tem-se $f_{c1} = 200KHz$ $\delta f1 = 25Hz$, 
em b) temos $f_{c2} = 64\times 200 = 12,8MHz$ e $\delta f2 = 64\times 25 = 1,6kHz$.

Se multiplicarmos tudo por 48 aconteceria o que já foi apresentado $f_c \approx 600MHz$.
Como queremos $f_c = 91,2MHz$ vamos abaixar $f_{c2}$ e modo que após a multiplicação 
por 48 tenhamos $f_c$.

A frequência em c) terá que ser 91,2MHz/48 = 1,9MHz para conseguirmos $f_{c3}$, a frequência 
do oscilador do conversor deve ser $f_{c2}\mp f_{c3}$, escolhemos 
$f_{c2}f_{c3} = 12,8 - 1,9 = 10,9MHz$.

O conversor de frequência não altera $\Delta f$ assim em c) temos $f_{c3} = 1,9MHz$ e
$\Delta_{f3} = 1,6KHz$.

A última multiplicação produz em d) então $f_{c4} = 1,9\times 48 = 91,2MHz$ e 
$\Delta_{f4} = 1,6\times48 = 76,8KHz$ como desejávamos. O resultado é então 
amplificado e transmitido.

Este esquema apresenta boa estabilidades de frequência, mas sofre pelo ruído 
inerente cusado pelo excesso de multiplicadores e da distrorção nas 
frequências mais baixas quando $\Delta f_m$ não é tão pequeno.

\begin{exemplo} Discuta a natureza da distorção inerente no gerador de DM indireto de
Armstrong
\end{exemplo}
\subsubsection{Solução:}

Dois tipos de distorção: em amplitude e de frequência. Para NBFM
\begin{eqnarray}
\varphi_{FM} &=& A[\cos\omega_{c}t - k_{f}a(t)\sin\omega_{c}t] \nonumber \\
&=& A E(t)\cos[\omega_{c}t + \theta(t)]
\end{eqnarray}
sendo 
\[
E(t) = \sqrt(1 + k_{f}^{2}a^{2}(t)) \ \ e\ \ \theta(t) = tan^{-1}[k_{f}a(t)]
\]
vemos que a amplitude do sinal modulado $AE(t)$ não é constante, e portanto gera uma
distorção na amplitude esta distorção no entanto não é grave pois pode
ser eliminada por limitadores de faixa de passagem discutidos mais a frente.

Agora para FM $\theta(t) = k_{f}a(t)$ mas $\theta(t) = tan^{-1}(k_{f}a(t))$ e
\[
\omega_i = \frac{d\theta(t)}{dt} = k_{f}(d\theta(t)/dt)\frac{1}{1 + k_{f}^{2}a^{2}(t)}
\]
sendo $d\theta(t)/dt = m(t)$ e
\[
\frac{1}{1 + k_{f}^{2}a^{2}(t)} = 1 - k_{f}^{2}a^{2}(t) + k_{f}^{4}a^{4}(t) ...
\]
assim
\[
\omega_i = k_{f}m(t)[1 - k_{f}^{2}a^{2}(t) + k_{f}^{4}a^{4}(t) ...]
\]
para o FM $\omega_i = k_{f}m(t)$, de modo que o restante dos temos da equação acima
representa a distorção.

Quando $m(t)=\alpha\cos\omega_{c}t$, $a(t) =\frac{\alpha}{\omega_m}\sin\omega_{m}t$ 
e
\[
\beta = \frac{\Delta f}{f_m} = \frac{\Delta\omega}{\omega_m} = \frac{\alpha_k{f}}{\omega_m}
\]
assim
\[
\omega_i = k_{f}\alpha\cos\omega_{m}t[1 - k_{f}^{2}\frac{\alpha^2}{\omega_m^2}\sin^2\omega_{m}t + 
k_{f}^{4}\frac{\alpha^4}{\omega_m^4}\sin^4\omega_{m}t ...]
\]
\[
\omega_i = \beta\omega_m\cos\omega_{m}t[1 - \beta^{2}\sin^{2}\omega_{m}^{2}t + 
\beta^{4}\sin^{4}\omega_{m}^{4}t ...]
\]

Esta equação como podemos observar, apresenta apenas harmônicos ímpares de distorção e 
a mais importante é a terceira harmônica. Ignorando os outros temos tem-se
\begin{eqnarray}
\omega_i &=& \beta\omega_m\cos\omega_{m}t[1 - \beta^{2}\sin^{2}\omega_m^{2}t] \nonumber \\
&=& \beta\omega_m\cos\omega_{m}t[1 - \frac{\beta^{2}}{2}(1 - \cos 2\omega_mt)] \nonumber \\
&=& \beta\omega_m\cos\omega_{m}t - \frac{\beta^{3}}{2}\omega_{m}\cos\omega_{m}t + 
\frac{\beta^{3}}{2}\omega_{m}\cos\omega_mt\cos2\omega_{m}t \nonumber \\
&=& (\beta - \beta^{3}/2)\omega_m\cos\omega_{m}t + \frac{\beta^{3}}{4}\omega_{m}(\cos\omega_{m}t + 
\cos3\omega_m t) \nonumber \\
&=& (\beta - \beta^{3}/4)\omega_m\cos\omega_{m}t + \frac{\beta^{3}}{4}\omega_{m}\cos3\omega_{m}t  \nonumber \\
&=& \beta\omega_{m}(1 - \beta^{2}/4)\cos\omega_{m}t + \frac{\beta^{3}}{4}\omega_{m}\cos3\omega_{m}t  \nonumber \\
para\ \ \beta << 1 \nonumber \\
\omega_i &=& \beta\omega_{m}\cos\omega_{m}t + \frac{\beta^{3}}{4}\omega_{m}\cos3\omega_{m}t
\end{eqnarray}
O primeiro temo após a igualdade é desejado e o segundo termo é a distorção.

 No exemplo anterior, o pior caso ocorria quando $f_m = 50Hz$ sendo $\beta = \frac{\Delta f}{f_m}
= \frac{25}{50} = 0,5$.

Nesse caso a razão dostorção da terceira harmônica pela desejada seria
\[
\frac{\beta^{3}\omega_{m}/4}{\beta\omega_m} = \frac{\beta^{2}}{4} = \frac{0,5^2}{4}\frac{1}{16}
\]
ou 6,25\%.

\subsection{Geração Direta}

Um VCO ({\em Voltage Controlled Oscillator}) é um dispositivo que controla a frequência
de saída por meio de uma ternão de entrada. A frequência de oscilação varia linearmente 
com o sinal de entrada. o VCO apresenta uma frequeência de repouso, quando a entrada é
zero, esta frequência chamaremos de $\omega_{c}$ então, como mostrado na Figura~\ref{fig:geracaodireta},
$\omega_i = \omega_c + k_fm(t)$.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/geracaodireta.eps,width=6cm}}
\caption{\label{fig:geracaodireta}VCO para gerar o sinal FM.}
\end{figure}
%*******************

Um VCO pode ser construído com um amplificador operacional e um comparador. Uma 
outra maneira é através da variação dos parâmetros reativos ($L$ ou $C$) de
um circuito ressonante de um oscilador.

\section{Demodulação de FM}

A informação em um sinal FM está na frequência instantânea
\[
\omega_i = \omega_c + k_fm(t)\
\]
portanto uma malha seletiva em frequência com função de transferência
dada por
\[
\mid H(\omega)\mid = a\omega + b
\]
na faixa de FM produziria uma saída proporcional a $\omega_i$.

A malha mais simples é um diferenciador ideal com função de transferência
$j\omega$. Aplicando $\varphi_{FM}(t)$ a um diferenciador ideal temos,
\begin{eqnarray}
\varphi_{FM}(t) &=& A\cos[\omega_c t + k_f\int_{-\infty}^{t}m(\alpha)d\alpha]
\nonumber \\
\frac{d\varphi_{FM}(t)}{dt} &=& -A[\omega_c + k_f m(t)]\sin[ ] = \varphi^{\prime}(t) \nonumber
\end{eqnarray}
o sinal $\varphi^{\prime}_{FM}(t)$ é um sinal modulado tanto em frequência como em 
amplitude, como está representado na Figura~\ref{fig:moduladofreqampl}.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/moduladofreqampl.eps,width=8cm}}
\caption{\label{fig:moduladofreqampl}Sinal $\varphi^{\prime}_{FM}(t)$.}
\end{figure}
%*******************

A envoltória de $\varphi^{\prime}_{FM}(t)$ é $A[\omega_c + k_fm(t)]$. Como
\[
\omega_c > k_fm(t) \Rightarrow A[\omega_c + k_fm(t)]>0, \ \ \forall t
\]
e portanto $m(t)$ pode ser obtida por detecção de envoltória. Assim o 
demodulador FM em diagrama de blocos está representado na Figura~\ref{fig:demodulafm}.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/demodulafm.eps,width=12cm}}
\caption{\label{fig:demodulafm}Diagramas de bloco do demodulador FM.}
\end{figure}
%*******************

Observe que a amplitude $A$ deve ser constante. Se a amplitude $A$ não for constante,
mas função do tempo, então
\begin{eqnarray}
\varphi_{FM}(t) &=& A(t)\cos(\omega_c t + \theta(t)] \ \ \theta(t) = k_f\int_{-\infty}^{t}m(\alpha)d\alpha \nonumber \\
\frac{d\varphi_{FM}}{dt} &=&-A(t)[\omega_c \theta^{\prime}(t)]\sin(\omega_c t + \theta(t))
+ \frac{dA}{dt}\cos(\omega_c t + \theta(t)) \nonumber
\end{eqnarray}
considerando que $A(t)$ varia muito lentamente, o termo $dA/dt$ pode ser desprezado,
mesmo assim a envoltória de $\varphi^{\prime}_{FM}$ seria
\[
E(t) = A(t)[\omega_c + \theta^{\prime}(t)] = A(t)[\omega_c + k_fm(t)]
\]
que é proporcional a $m(t)A(t)$. Portanto é essencial manter $A$ constante.

Vários fatores como ruído e desvanecimento fazem com que $A$ varie. Essa variação deve ser removida antes de aplicar o sinal FM ao detetor.

\subsection{Limitador de Banda Passante}

As variações em amplitude podem ser eliminadas por um limitador na banda passante, como está 
ilustrado na Figura~\ref{fig:limitadorbrusco}.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/limitadorbrusco.eps,width=8cm}}
\caption{\label{fig:limitadorbrusco}Limitador Brusco na banda passante.}
\end{figure}
%*******************

O limitador brusco tem a seguinte caracterísitca
\begin{equation}
v_0 = \left\{
\begin{array}{c c}
1 & se\ v_i>0 \\
-1 & se\ v_i < 0 
\end{array}
\right.
\end{equation}


Observe que a saída do limitador brusco a uma senóide será uma onda quadrada de amplitude 1 independente da amplitude da senóide. Além disso, os cruzamentos no zero são preservados pois quando a entrada é zero a saída também é zero.

Assim se $v_1(t) = A(t)\cos\theta(t)\Rightarrow v_0(t)$ tem a amplitude constante

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/saidalimitadorbrusco.eps,width=8cm}}
\caption{\label{fig:saidalimitadorbrusco}Saída do limitador Brusco na banda passante.}
\end{figure}
%*******************

Quando $v_0(t)$ passa pelo filtro passa-faixas centrado em $\omega_c$ a saída é um sinal modulado em ângulo. Vejamos
\[
v_i(t) = A(t)\cos\theta(t)
\]
em que
\[
\theta(t) = \omega_c t + k_f\int_{-\infty}^{t}m(\alpha)\alpha
\]
$v_0(t)$ depende do sinal de $v_i(t)$. Considerando $A(t)\geq 0 $ $v_0(t)$ pode ser expresso por
\begin{equation}
v_0(\theta) = \left\{
\begin{array}{c c}
1 & se\ \cos\theta>0 \\
-1 & se\ \cos\theta < 0 
\end{array}
\right.
\end{equation}

Assim como função de $\theta$ é uma função quadrada periódica com período $2\pi$

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/voteta.eps,width=8cm}}
\caption{\label{fig:voteta}Sinal $v_o(\theta)$}
\end{figure}
%*******************

que pode ser expandido em série de Foutier
\[
v_0(\theta) = \frac{4}{\pi}(\cos\theta - \frac{1}{3}\cos 3\theta + \frac{1}{5}\cos 5\theta - ..)
\]
que é válida para qualquer $\theta$ real.

Em qualquer instante de tempo $t$
\[
\theta(t) = \omega_c t + k_f\int_{-\infty}^{t}m(\alpha)d\alpha
\]
assim
\[
v_0(t) = \frac{4}{\pi}\left\{\cos[\omega_c t + k_f\int_{-\infty}^{t}m(\alpha)d\alpha] - \frac{1}{3}\cos[3\omega_c t + 3k_f\int_{-\infty}^{t}m(\alpha)d\alpha)] + ....\right\}
\]
passando pelo filtro passa-faixas centrado em $\omega_c$ tem-se
\[
e_0(t) =\frac{4}{\pi}\cos[\omega_c t + k_f\int_{-\infty}^{t}m(\alpha)d\alpha]
\]
Esse resultado também pode ser aplicado para sinais PM.

Além de manter a amplitude constante o limitador na banda passante suprime o ruído quando esse é pequeno.

\section{Interferência em Sistemas Modulados em Ângulo}

Vamos considerar o caso simples da interferência de uma portadora não modulada $A\cos\omega_c t$ por uma outra senóide $I\cos(\omega_c + \omega)t$, como está representado na Figura~\ref{fig:interferenciasenoidal}.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/interferenciasenoidal.eps,width=4cm}}
\caption{\label{fig:interferenciasenoidal}Interferência senoidal}
\end{figure}
%*******************

\begin{eqnarray}
r(t) &=& A\cos\omega_c t + I \cos(\omega_c + \omega)t \nonumber \\
&=& A\cos\omega_c t + I\cos\omega_c t\cos\omega t - I\sin\omega_c t\sin\omega t \nonumber \\
&=& (A + I\cos\omega t)\cos\omega_c t - I\sin\omega_c t\sin\omega t \nonumber \\
&=& E_{A}(t)\cos(\omega_c(t) + \phi_{d}(t)) \nonumber
\end{eqnarray}
em que
\[
\phi_{d}(t) = \tan^{-1}\frac{I\sin\omega t}{A+I\cos\omega t}
\]

Quando o sinal de interferência é pequeno em comparação a portadora $(I<<A)$
\[
\phi_d(t)\approx \frac{I}{A}\sin\omega t
\]

A frequência instantâneaa de $r(t)$ é $\omega_i = \omega_c + \phi_d^{\prime}(t)$
aplicando $r(t)$ a um demodulador de fase ideal, a saída do demodulador será
\[
y_{d}(t) = \phi_{d}(t) \approx \frac{I}{A}\sin\omega t
\]
similarmente para um demodulador FM ideal
\[
y_d(t) = \frac{d\phi_d}{dt} = \frac{I}{A}\omega\cos\omega t
\]
observe que em qualquer caso a influência é inversamente proporcional a amplitude da portadora $A$. Ou seja, quanto maior a amplitude da portadora $A$ menor o efeito da intrferência.

Esse efeito é muito diferente das sinais AM, em que a interferência na saída do demodulador é independente de amplitude da portadora.


%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/interferenciaam.eps,width=4cm}}
\caption{\label{fig:interferenciaam}Interferência no sinal AM.}
\end{figure}
%*******************

Como exemplo seja a interferência na Figura~\ref{fig:interferenciaam}.
\begin{eqnarray}
r(t) &=& \varphi(t) + I\cos(\omega_c + \omega)t \nonumber \\
&=& [a+m(t)]\cos\omega_c t + I\cos(\omega_c + \omega)t \nonumber \\
&=& [A + m(t) + I\cos\omega t]\cos\omega_c t - I\sin\omega t\sin\omega_c t \nonumber
\end{eqnarray}

A envoltório desse sinal será
\begin{eqnarray}
E(t) &=& \{[A + m(t) + I\cos\omega\omega t]^2 + I ^2\sin^2\omega t\}^{1/2} \nonumber \\
&\approx& A + m(t) + I\cos\omega t\ quando\ I<<A \nonumber
\end{eqnarray}

Ou seja, o sinal de interferência na saída do detetor de envoltória é $I\cos\omega t$, que é independente da amplitude da portadora $A$. O mesmo resultado é obtido para demodulação síncrona. E resultados similares para os sistemas sem portadora.

Portanto, os sistemas modulados em ângulo suprime as interferências fracas ($I<<<A$) melhor que os sistemas AM.

A Figura~\ref{fig:efeitointerferenciafmepm} mostra o efeito da interferência em FM e PM.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/efeitointerferenciafmepm.eps,width=8cm}}
\caption{\label{fig:efeitointerferenciafmepm}Efeito da interferência nos sinais FM e PM.}
\end{figure}
%*******************

O ruído no canal age como uma interferência nos sinais modulados em ângulo.

\subsection{Preênfase e Deênfase}

A Figura acima mostra que para FM a interferência é mais severa para frequências mais altas do sinal interferência.

Como o ruído se comporta como uma interferência a potência do ruído no receptor está concentrado nas frequências mais altas.

Se $m(t)$ for uma sinal a potência desse sinal está concentrada nas frequências mais altas.

Dessa forma a DEP do ruído está centrada onde $m(t)$ é mais fraco, o que parece destroso.

Na realidade pe uma oportunidade de reduzir fortemente o ruído. Esse processo é mostrado na 
Figura~\ref{fig:preenfase}.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/preenfase.eps,width=12cm}}
\caption{\label{fig:preenfase}Filtro Preênfase.}
\end{figure}
%*******************

O filtro $H_p(\omega)$ no transmissor incrementa as frequências mais altas (que tem potência mais baixa) de $m(t)$ antes da modulação. Esse filtro é chamado de filtro de preênfase.

No receptor, a saída do demodulador é passada por um filtro de deênfase com função de transferência $H_0(\omega) = 1/H_p(\omega)$.

Dessa forma o filtro de deênfase desfaz o filtro de preênfase atenuando as componenentes de frequências mais altas.

O ruído entra no canal e portanto não passa pelo filtro de pré-ênfase. No entanto, ele passa através do filtro de de-ênfase, que atenua as frequências mais altas, nas quais a potêncis do ruído está concentrada.

Desse modo, o processo de pré-ênfase e de-ênfase deixa e sinal $m(t)$ intocável mas reduz consideravelmente a potência do ruído.

\subsection{Receptor FM}

A FCC atrtibuiu a faixa de frequências 88 a 108MHz para FM, com separação de 200KHz entre estações
adjacentes e desvio de frequência $\Delta f = 75KHz$ e 200KHz de largura de faixa.

Um receptor FM monofônico é idêntico ao receptor AM superheterodino, a frequência intermediária
é de 10,7MHz e o detector de envoltória é substituído por um PLL ou um disciminador
de frequência seguido por um filtro de deênfase.

As  primeiras transmissões FM eram monofônicas. Transmissões estéreo,  usando dois canais
de áudio, E (microfone esquerdo) e D (microfone direito), foram propostas mais 
tarde.

O novo sistema tinha que ser compatível com o antigo, ou seja, o sistema antigo deveria
ser capaz de receber o sinal $E+D$ e a largura de faixa total dos dois sinais $E$ e $D$
deveria ocupar ainda 200KHz com $\Delta f = 75KHz$.

Um transmissor e receptor FM estéreo é mostrado na Figura~\ref{fig:fmestereo}.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/fmestereo.eps,width=12cm}}
\caption{\label{fig:fmestereo}FM estéreo.}
\end{figure}
%*******************

O espectro em 1 é o seguinte:
\[
m(t) = (L+R)^{\prime} + (L-R)^{\prime} + \alpha\cos\omega_c t/2
\]
$\omega_c = 2\pi\times 38 rad/s$


O receptor está representado na Figura~\ref{fig:receptorfmestereo}, sendo que
o receptor monofônico consiste apenas do ramo de cima.

%*******************
\begin{figure}[h]
\centerline{\psfig{figure=./Figuras/receptorfmestereo.eps,width=12cm}}
\caption{\label{fig:receptorfmestereo}Receptor FM estéreo.}
\end{figure}
%*******************

O valor de pico $m_p$ do sinal $m(t)$ fica praticamente inalterado de modo que 
$\Delta f$ permanece praticamente o mesmo.

